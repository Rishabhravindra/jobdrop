extends layout

block content
  .reg-container.container
    h1.h1-responsive.text-xs-center Add Job
    form#registerForm.reg-form.py-1(method='POST' action='/add')
      .form-inline
        .md-form.form-group
          input#name.form-control(type='text', name='link')
          |  
          label(for='link') Enter URL (http://..)
        button.btn.btn-primary.btn-submit.pt-1.waves-effect.waves-light(type='submit') Submit

        
